version: 0.2

phases:
  install:
    commands:
      - cd frontend
      - npm install
      - npm install -g @angular/cli
  build:
    commands:
      - ng build --configuration staging
artifacts:
    files:
        - '**/*'
    base-directory: 'dist/frontend/browser/*'
    discard-paths: yes